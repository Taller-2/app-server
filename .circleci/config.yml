version: 2.1

jobs:
  test:
    docker:
      - image: circleci/python:3.6.6
    steps:
      - checkout
      - run: "pip install -r requirements.txt"
      - run: "sh scripts/tests.sh"

workflows:
    "Run unit tests":
        jobs:
          - test